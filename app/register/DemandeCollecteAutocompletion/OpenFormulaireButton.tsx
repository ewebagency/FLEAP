/*import React from 'react';
import BoxIcon from "@/app/component/BoxIconWrapper";

interface OpenFormulaireButtonProps {
  onClick: () => void;
}

const OpenFormulaireButton: React.FC<OpenFormulaireButtonProps> = ({ onClick }) => {
  return (
    <button
      onClick={onClick}
      className="flex items-center gap-2 px-4 py-2 bg-[var(--green-medium)] text-white rounded-md hover:bg-[var(--green-dark)] transition-colors duration-200"
    >
      <BoxIcon name="truck" type="solid" />
      <span>Nouvelle demande de collecte</span>
    </button>
  );
};

export default OpenFormulaireButton; */